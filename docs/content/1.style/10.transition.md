---
head:
  meta:
    - name: 'stage'
      content: 'alpha'
navigation:
  stage: 'alpha'
---
# Transition

:stage{type="alpha"}

过渡动画组件，目前仅支持 fadeInUp。**组件挂载时会自动执行一次过渡动画**。可用于页面进入时的元素入场动画。

```ts
import { DuTransition } from 'dangoui'
```

## 说明

1. 目前仅有 fadeInUp
2. 当 DuTransition 挂载的时候，会执行一次过渡动画
